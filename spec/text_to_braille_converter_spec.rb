require 'spec_helper'

describe TextToBrailleConverter do
  it 'has a version number' do
    expect(TextToBrailleConverter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
